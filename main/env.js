// All secrets MUST come from .env at the project root (loaded by main/main.js).
// Do NOT hard-code real API keys here — this file is committed to git and any
// key placed here will be auto-revoked by Google/OpenRouter scanners within
// minutes of being pushed to a public repository.
//
// Only non-secret defaults (public URLs, app name) belong here.
module.exports = {
  NEXT_PUBLIC_APP_NAME: "EduPlatform",
  NEXT_PUBLIC_APP_URL: "http://localhost:3000/",
};
